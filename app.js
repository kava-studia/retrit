const MAX_PHONE='+79057195398';const MAX_WEB='https://max.ru/';const assetMap={logo:'/assets/logo.b64',hero:'/assets/hero.b64',real:'/assets/real.b64'};async function loadAsset(name){try{const b64=(await fetch(assetMap[name],{cache:'force-cache'}).then(r=>r.text())).trim();const url='data:image/webp;base64,'+b64;document.querySelectorAll('[data-img="'+name+'"]').forEach(el=>el.src=url)}catch(e){console.warn(name,e)}}Object.keys(assetMap).forEach(loadAsset);const toast=document.getElementById('toast');function showToast(){toast.classList.add('show');clearTimeout(window.__t);window.__t=setTimeout(()=>toast.classList.remove('show'),3200)}async function buy(){try{await navigator.clipboard.writeText(MAX_PHONE)}catch(e){const t=document.createElement('textarea');t.value=MAX_PHONE;document.body.appendChild(t);t.select();document.execCommand('copy');t.remove()}showToast();setTimeout(()=>window.open(MAX_WEB,'_blank','noopener'),350)}document.querySelectorAll('.max-cta').forEach(x=>x.addEventListener('click',buy));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));